class MessageObject {
    constructor(createdAt, userFrom, userTo, text) {
        this.createdAt = createdAt;
        this.userFrom = userFrom;
        this.userTo = userTo;
        this.text = text;
    }
}